{                          //TAG TYPE    
                           //    this is in addition to request header.
							     
	                       //    the GW will set only the value that has been passed.
    "USR": "username",     //USR C_USERNAME string, the user to be used for connecting the mqtt broker. zeroed if null
    "PWD": "secret"        //PWD C_PASSWORD string, the password to be used for connecting the mqtt broker. zeroed if null
}
